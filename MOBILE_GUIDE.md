# 📱 دليل التشغيل على الموبايل (محدث)

يمكنك تشغيل البوت مباشرة من هاتفك المحمول باستخدام إحدى الطرق التالية.

---

## 🌐 الطريقة الأولى: Replit (الأسهل والأفضل للمبتدئين)

**Replit** هي منصة برمجة سحابية تتيح لك تشغيل المشاريع مباشرة من المتصفح أو التطبيق.

### الخطوات:

1. **إنشاء حساب:**
   - اذهب إلى [replit.com](https://replit.com) من متصفح الموبايل.
   - أنشئ حساباً جديداً (يمكنك استخدام Google أو GitHub).

2. **إنشاء مشروع جديد:**
   - اضغط على "Create Repl".
   - اختر "Node.js" كنوع المشروع.
   - أعطه اسماً مثل "school-bot".

3. **رفع الملفات:**
   - استخدم زر "Upload file" لرفع جميع ملفات المشروع.
   - أو انسخ محتوى كل ملف والصقه في ملف جديد.

4. **إضافة المتغيرات البيئية:**
   - في الشريط الجانبي، اضغط على "Secrets" (أيقونة القفل).
   - أضف المتغيرات التالية:
     - `DISCORD_TOKEN`: توكن البوت
     - `CLIENT_ID`: Application ID
     - `AI_API_KEY`: مفتاح OpenAI (اختياري)
     - `TEACHER_ROLE_NAME`: اسم رتبة المعلمين
     - **`QUIZ_CHANNEL_ID`**: ID قناة الاختبارات اليومية **(جديد)**

5. **تثبيت الاعتماديات:**
   - افتح "Shell" (الطرفية) في Replit.
   - اكتب الأمر:
     ```bash
     npm install
     ```

6. **تشغيل البوت:**
   - اضغط على زر "Run" الأخضر في الأعلى.
   - سيبدأ البوت بالعمل!

### ✅ المميزات:
- سهل جداً للمبتدئين.
- لا يحتاج تثبيت أي شيء على الموبايل.
- يمكنك تعديل الكود مباشرة من المتصفح.

### ⚠️ ملاحظات:
- الخطة المجانية تتوقف بعد فترة من عدم النشاط (يمكنك استخدام خدمة Uptime Robot لإبقائه يعمل).
- للتشغيل الدائم 24/7، قد تحتاج للترقية للخطة المدفوعة.

---

## 📱 الطريقة الثانية: Termux (Android فقط)

**Termux** هو تطبيق محاكي طرفية (Terminal) لنظام Android يتيح لك تشغيل Node.js مباشرة على هاتفك.

### الخطوات:

1. **تثبيت Termux:**
   - حمّل Termux من [F-Droid](https://f-droid.org/en/packages/com.termux/) (لا تستخدم Google Play).
   - افتح التطبيق.

2. **تحديث الحزم:**
   ```bash
   pkg update && pkg upgrade
   ```

3. **تثبيت Node.js:**
   ```bash
   pkg install nodejs-lts
   ```

4. **تثبيت Git (اختياري):**
   ```bash
   pkg install git
   ```

5. **نقل ملفات المشروع:**
   - يجب أن تكون ملفات المشروع في مجلد واحد (مثلاً `school-bot`) في ذاكرة الهاتف.
   - في Termux، اكتب:
     ```bash
     termux-setup-storage
     cd /storage/emulated/0/school-bot
     ```
     (استبدل المسار بمسار مجلدك).

6. **تثبيت الاعتماديات:**
   ```bash
   npm install
   ```

7. **إنشاء ملف .env:**
   - استخدم محرر نصوص مثل `nano`:
     ```bash
     nano .env
     ```
   - الصق المتغيرات البيئية (مع إضافة `QUIZ_CHANNEL_ID`):
     ```
     DISCORD_TOKEN=your_token_here
     CLIENT_ID=your_client_id
     AI_API_KEY=your_openai_key
     TEACHER_ROLE_NAME=Teacher
     QUIZ_CHANNEL_ID=your_quiz_channel_id
     ```
   - احفظ بالضغط على `Ctrl + X` ثم `Y` ثم `Enter`.

8. **تشغيل البوت:**
   ```bash
   npm start
   ```

### ✅ المميزات:
- تحكم كامل في البوت.
- لا يحتاج اتصال إنترنت مستمر (إلا للبوت نفسه).
- مجاني بالكامل.

### ⚠️ ملاحظات:
- يحتاج معرفة أساسية بالطرفية (Terminal).
- البوت يعمل فقط عندما يكون Termux مفتوحاً.
- قد يستهلك بطارية الهاتف.

---

## ☁️ الطريقة الثالثة: خدمات الاستضافة المجانية

يمكنك استخدام خدمات استضافة مجانية لتشغيل البوت 24/7:

### 1. **Render.com**
- اذهب إلى [render.com](https://render.com).
- أنشئ حساباً.
- اختر "New Web Service".
- اربط حسابك بـ GitHub ورفع المشروع.
- أضف المتغيرات البيئية في إعدادات الخدمة (بما في ذلك `QUIZ_CHANNEL_ID`).

### 2. **Railway.app**
- اذهب إلى [railway.app](https://railway.app).
- أنشئ حساباً.
- اختر "New Project" > "Deploy from GitHub".
- أضف المتغيرات البيئية (بما في ذلك `QUIZ_CHANNEL_ID`).

### 3. **Glitch.com**
- شبيه بـ Replit.
- اذهب إلى [glitch.com](https://glitch.com).
- أنشئ مشروع Node.js جديد.
- ارفع الملفات وأضف المتغيرات.

---

## 🔄 إبقاء البوت يعمل 24/7

إذا كنت تستخدم خدمة مجانية قد تتوقف بعد فترة، استخدم **Uptime Robot**:

1. اذهب إلى [uptimerobot.com](https://uptimerobot.com).
2. أنشئ حساباً.
3. أضف "New Monitor" من نوع HTTP(s).
4. ضع رابط مشروعك (مثلاً من Replit).
5. سيقوم بزيارة الرابط كل 5 دقائق لإبقاء البوت نشطاً.

---

## 🆘 حل المشاكل الشائعة

### البوت لا يظهر أونلاين:
- تأكد من صحة `DISCORD_TOKEN`.
- تأكد من تفعيل جميع الـ Intents في Developer Portal.

### الأوامر لا تظهر:
- تأكد من إضافة `CLIENT_ID` في ملف `.env`.
- انتظر بضع دقائق (قد يستغرق Discord وقتاً لتحديث الأوامر).

### خطأ في AI:
- تأكد من صحة `AI_API_KEY`.
- تأكد من وجود رصيد في حساب OpenAI.

---

## 📞 الدعم

إذا واجهت أي مشكلة، يمكنك:
- مراجعة ملف `README.md` للمزيد من التفاصيل.
- مراجعة هذا الدليل.
- البحث عن الخطأ في Google.

---

**تم التطوير بواسطة Manus AI** 🤖
