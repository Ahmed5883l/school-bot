> مشروع بوت Discord تعليمي متكامل تم تطويره بواسطة Manus AI.

# 🤖 بوت المدرسة (School Bot)

بوت Discord تعليمي متكامل مصمم لمساعدة المعلمين والطلاب في السيرفرات التعليمية. يوفر البوت مجموعة من الميزات التلقائية لتسهيل عملية التعلم والتفاعل.

## ✨ الميزات الرئيسية

- **📚 نظام الدروس المجدولة:** نشر الدروس تلقائياً في قنوات محددة بناءً على جدول زمني (Cron).
- **🎫 نظام التذاكر الذكي:** إنشاء قنوات دعم خاصة للطلاب مع ردود تلقائية من مساعد AI لمساعدة الطلاب.
- **🏆 نظام الخبرة والرتب (XP):** منح نقاط خبرة (XP) للطلاب على تفاعلهم وترقيتهم تلقائياً إلى رتب جديدة.
- **📝 نظام الاختبارات التلقائي:** إنشاء اختبارات تفاعلية (MCQ) حول أي موضوع باستخدام الذكاء الاصطناعي وتصحيحها تلقائياً.
- **🗓️ اختبارات يومية تلقائية:** إرسال اختبار جديد يومياً في قناة محددة مع منح XP للإجابات الصحيحة.
- **🧠 مساعد ذكاء اصطناعي عام:** تحسين وظيفة AI ليكون مساعداً عاماً للإجابة على أسئلة المستخدمين في التذاكر.
- **🛡️ أوامر الإدارة:** أوامر متقدمة للمشرفين والمعلمين لإدارة الأعضاء (كتم، تبنيد، تعيين رتب).
- **👨‍🏫 صلاحيات المعلمين:** أوامر خاصة للمعلمين لإدارة الدروس، الاختبارات، ومنح النقاط.
- **📊 لوحة صدارة:** عرض قائمة بالطلاب الأكثر تفاعلاً ونشاطاً في السيرفر.
- **⚙️ أوامر تفاعلية:** استخدام أحدث أوامر Slash وأزرار Discord لتجربة مستخدم سهلة.

---

## 🚀 كيفية التشغيل

اتبع الخطوات التالية لتشغيل البوت على سيرفرك الخاص.

### 1. المتطلبات الأساسية

- **Node.js:** إصدار 16.9.0 أو أحدث.
- **حساب Discord:** لإنشاء البوت والسيرفر.
- **مفتاح OpenAI API (اختياري):** لتفعيل ميزات الذكاء الاصطناعي.

### 2. إعداد البوت في Discord

1.  **إنشاء تطبيق جديد:** اذهب إلى [Discord Developer Portal](https://discord.com/developers/applications) وأنشئ تطبيقاً جديداً.
2.  **الحصول على Token:** في صفحة "Bot"، اضغط على "Reset Token" لإنشاء توكن جديد وانسخه. **لا تشارك هذا التوكن مع أحد!**
3.  **الحصول على Client ID:** في صفحة "General Information"، انسخ "Application ID".
4.  **تفعيل Intents:** في صفحة "Bot"، قم بتفعيل الخيارات التالية:
    - `PRESENCE INTENT`
    - `SERVER MEMBERS INTENT`
    - `MESSAGE CONTENT INTENT`
5.  **دعوة البوت:** اذهب إلى "OAuth2" > "URL Generator". اختر الصلاحيات التالية:
    - `bot`
    - `applications.commands`
    - **Bot Permissions:**
        - `Send Messages`
        - `Manage Channels`
        - `Manage Roles`
        - `Moderate Members` (للكتم)
        - `Ban Members` (للتبنيد)
        - `Read Message History`
        - `Embed Links`
        - `Attach Files`
    - انسخ الرابط الذي تم إنشاؤه وادخل عليه لدعوة البوت إلى سيرفرك.

### 3. إعداد المتغيرات

1.  **تنزيل المشروع:** قم بتنزيل ملفات المشروع.
2.  **تثبيت الاعتماديات:** افتح الطرفية (Terminal) في مجلد المشروع وقم بتشغيل الأمر:
    ```bash
    npm install
    ```
3.  **إعداد المتغيرات:**
    - قم بإعادة تسمية ملف `.env.example` إلى `.env`.
    - افتح ملف `.env` واملأ المتغيرات المطلوبة:
        - `DISCORD_TOKEN`: توكن البوت الذي نسخته.
        - `CLIENT_ID`: الـ Application ID الخاص بالبوت.
        - `AI_API_KEY`: مفتاح OpenAI API (إذا كنت تريد استخدام AI).
        - `TEACHER_ROLE_NAME`: اسم رتبة المعلمين في سيرفرك (مثال: `Teacher`).
        - `LESSONS_CHANNEL_ID`: ID القناة التي تريد نشر الدروس فيها.
        - **`QUIZ_CHANNEL_ID`**: ID القناة التي تريد إرسال الاختبارات اليومية إليها.

### 4. تشغيل البوت

- لتشغيل البوت، استخدم الأمر:
  ```bash
  npm start
  ```
- للتطوير (مع إعادة تشغيل تلقائي عند التعديل):
  ```bash
  npm run dev
  ```

---

## 📖 دليل الأوامر

### 👥 أوامر عامة للطلاب

- `/ticket`: إنشاء تذكرة دعم جديدة لطرح سؤال.
- `/level [user]`: عرض مستواك ونقاطك أو مستوى مستخدم آخر.
- `/leaderboard`: عرض لوحة الصدارة لأكثر الطلاب نشاطاً.
- `/help`: عرض قائمة بجميع الأوامر المتاحة.
- `/info`: عرض معلومات عن البوت.

### 👨‍🏫 أوامر خاصة بالمعلمين

- `/addlesson`: إضافة درس جديد ليتم نشره تلقائياً.
- `/quiz`: إنشاء اختبار تفاعلي جديد للطلاب.
- `/addxp [user] [amount] [allowed_role]`: إضافة نقاط خبرة (XP) لمستخدم بشكل يدوي.

### 🛡️ أوامر الإدارة (للمشرفين والمعلمين)
**ملاحظة:** تم إضافة خيار `allowed_role` لجميع أوامر الإدارة و `/addxp`، مما يسمح لك بتحديد رتبة معينة يمكنها استخدام الأمر. إذا لم تحدد رتبة، فسيتم تطبيق الصلاحيات الافتراضية (مثل `Manage Roles`).

- `/setrole [user] [role] [allowed_role]`: تعيين رتبة لعضو. (يمكنك تحديد `allowed_role` للتحكم في من يمكنه استخدام الأمر)
- `/removerole [user] [role] [allowed_role]`: إزالة رتبة من عضو.
- `/mute [user] [duration] [reason] [allowed_role]`: كتم عضو لفترة محددة.
- `/unmute [user] [allowed_role]`: إلغاء كتم عضو.
- `/ban [user] [reason] [allowed_role]`: تبنيد عضو من السيرفر.

---

## 🔧 هيكل المشروع

```
/school-bot
├── 📂 data/              # ملفات قواعد البيانات (JSON)
│   ├── lessons_db.json
│   ├── quiz_db.json      # قاعدة بيانات الاختبارات
│   └── xp_db.json
├── 📂 modules/           # الوحدات البرمجية الأساسية
│   ├── ai.js             # التعامل مع OpenAI API (مساعد عام)
│   ├── commands.js       # تسجيل وإدارة الأوامر
│   ├── interactions.js   # معالجة التفاعلات (أوامر، أزرار)
│   ├── lessons.js        # نظام الدروس المجدولة
│   ├── moderation.js     # أوامر الإدارة (جديد)
│   ├── quiz.js           # نظام الاختبارات اليومية والتفاعلية (محدث)
│   ├── tickets.js        # نظام التذاكر (محدث)
│   └── xp.js             # نظام الخبرة والرتب
├── .env.example          # ملف مثال للمتغيرات البيئية (محدث)
├── .gitignore            # الملفات التي يتم تجاهلها
├── index.js              # نقطة انطلاق البوت الرئيسية (محدث)
├── package.json          # معلومات المشروع والاعتماديات
└── README.md             # هذا الملف (محدث)
```

## 💡 أفكار للتطوير المستقبلي

- لوحة تحكم ويب لإدارة البوت بسهولة.
- ربط مع قواعد بيانات أكثر قوة مثل MongoDB أو Supabase.
- نظام تصحيح تلقائي للأسئلة المفتوحة باستخدام AI.
- المزيد من الألعاب التعليمية التفاعلية.
